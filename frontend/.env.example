# ============================================
# FRONTEND ENVIRONMENT VARIABLES
# ============================================
# In production, API is proxied via nginx.
# Only needed if backend is on a separate domain.

# VITE_API_URL=http://your-backend-domain.com/api
